I've approved a redesign of yarddoglandscapes.com and have a handoff package ready. Repo: millermaines/Yard-Dog-Website (static HTML deployed on Vercel from main). The package is the folder `design_handoff_yard_dog_redesign/` — I'm putting it in the repo root.

Please:
1. Read `design_handoff_yard_dog_redesign/README.md` first. It explains the package and the design rules.
2. Create a branch `redesign-2026`.
3. Run `node design_handoff_yard_dog_redesign/apply-redesign.mjs --dry`, review the output, then run it for real.
4. Contrast check (required): after the script runs, serve the site and, for every .html page, walk all visible text nodes and flag any where the text color and the nearest opaque ancestor background have a relative luminance difference below 0.3 (ignore elements inside a section that has an absolutely-positioned photo <img>). Print failures per page. There should be none; if any appear, fix in redesign.css.
5. Sanity check: `git diff --stat`; confirm no page lost its <title>, canonical, or JSON-LD; open index.html, contact.html, landscaping.html, hardscaping-tyler-tx.html and one blog post in a browser (npx serve .) and confirm the new header/footer/hero render, the Platy form (id k10wty88jnyk6dfl2jsmve) loads on / and /contact, and the nav dropdown + hamburger work.
6. MOBILE PASS (required): with the site served locally, use Playwright or Chrome DevTools device emulation at 390×844 (iPhone) and 768×1024 (iPad) on EVERY .html page in the repo root — script it, don't sample. For each page assert: no horizontal overflow (document.documentElement.scrollWidth <= window.innerWidth), no element wider than the viewport, headline text doesn't clip, tap targets ≥44px for .btn/nav links, hamburger opens the nav and every top-level link is reachable, the Platy iframe is visible on / and /contact. Save a screenshot per page to qa/mobile/<page>.png and print a list of any failures. Fix failures in redesign.css (global) — only touch a page's markup if the problem is unique to it. Re-run until clean.
7. If a sub-page hero picked a bad photo, override it with an inline background-image on its .page-hero. If any page's layout broke, fix it in redesign.css (not by editing the page) so the fix applies everywhere. Don't touch copy, URLs, or SEO markup.
8. Commit ("Redesign 2026: new header/footer/heroes, rebuilt core pages, redesign.css"), push, open a PR against main, and paste me the Vercel preview URL. Don't merge until I've looked at the preview.
